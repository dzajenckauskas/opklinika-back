'use strict';

/**
 * reimbursement-percentage service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reimbursement-percentage.reimbursement-percentage');
